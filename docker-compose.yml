services:
  harness:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: python -m literal.cli dev --host 0.0.0.0 --port 8787 --no-browser
    ports:
      - "8787:8787"
    environment:
      PYTHONUNBUFFERED: "1"
